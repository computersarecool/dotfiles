[user]
	email = willy@willynolan.com
	name = computersarecool
[core]
	editor = emacsclient
[push]
	default = matching
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
